const Discord = require("discord.js");
const wait = require('util').promisify(setTimeout);

module.exports = {
 name: "test",
 description: "For Turtlepaw!",
 async execute(message, Member, args) {

 },
};